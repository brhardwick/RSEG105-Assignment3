DROP DATABASE RSEG105;
CREATE DATABASE RSEG105;
USE RSEG105;

CREATE TABLE IF NOT EXISTS `RSEG105`.`BOOK` (
    `ID` INT NOT NULL  AUTO_INCREMENT PRIMARY KEY,
    `CATEGORY_NAME` VARCHAR(100) NULL,
    `ISBN` VARCHAR(10) NULL,
    `TITLE` VARCHAR(500) NULL,
    `PUBLISHER` VARCHAR(100) NULL,
    `PRICE` DECIMAL(4,2) NULL 
)  ENGINE=INNODB;
INSERT INTO BOOK(TITLE,ISBN) VALUES ('TEST' ,'1234567');
INSERT INTO BOOK(TITLE,ISBN) VALUES ('TEST2','1234567');
INSERT INTO BOOK(TITLE,ISBN) VALUES ('TEST3','9876543');